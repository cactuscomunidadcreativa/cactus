import { Suspense } from 'react'
import { AdvisorPage } from '@/modules/cereus/components/advisor-page'

export default function CereusAdvisorRoute() {
  return <Suspense><AdvisorPage /></Suspense>
}
